# Vertex attribute table for graph-like objects

Vertex attribute table for graph-like objects

## Usage

``` r
vdata(object)
```

## Arguments

- object:

  Graph-like object (for example an `igraph`).

## Value

Data frame of vertex attributes.
