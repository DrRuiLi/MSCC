# Convert isotope notation to element symbol

Convert isotope notation to element symbol

## Usage

``` r
get_ele_uniso(iso_ele = "[13]C")
```

## Arguments

- iso_ele:

  Isotope notation such as `"[13]C"` or `"C"`.

## Value

Element symbol without isotope mass prefix.
