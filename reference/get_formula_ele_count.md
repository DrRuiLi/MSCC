# Count an element in formula

Count an element in formula

## Usage

``` r
get_formula_ele_count(formula, ele = "C")
```

## Arguments

- formula:

  Chemical formula.

- ele:

  Element symbol or isotope notation.

## Value

Integer vector of element counts.
