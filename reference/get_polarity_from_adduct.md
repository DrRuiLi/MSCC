# Infer polarity from adduct expression

Infer polarity from adduct expression

## Usage

``` r
get_polarity_from_adduct(adduct)
```

## Arguments

- adduct:

  Character adduct annotation, e.g. `"[M+H]+"`, `"[M-H]-"`.

## Value

Integer polarity (`0` negative, `1` positive).
