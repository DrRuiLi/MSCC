# Normalize CFM adduct input to `"[M+H]+"` / `"[M-H]-"`

Accepts polarity shorthand: `0`/`"0"`/`"-"` -\> `"[M-H]-"`;
`1`/`"1"`/`"+"` -\> `"[M+H]+"`.

## Usage

``` r
.normalize_cfm_param_adduct(param_adduct)
```
