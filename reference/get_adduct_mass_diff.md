# Compute pairwise adduct mass differences by polarity

Compute pairwise adduct mass differences by polarity

## Usage

``` r
get_adduct_mass_diff(pol = 0)
```

## Arguments

- pol:

  Polarity (`0`/`1`, `"negative"`/`"positive"`).

## Value

Data frame with adduct pairs and delta mass.
