# chemform_parse

parse given chemform to a vector of elements and count

## Usage

``` r
chemform_parse(chemform = MSCC::demo_chemform, return = "matrix")
```

## Arguments

- chemform:

  formula

- return:

  return type

## Value

ele matrix of list
