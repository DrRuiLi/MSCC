# Build atom map from fmcsR mcs object

Build atom map from fmcsR mcs object

## Usage

``` r
get_mcs_atom_map(mcs)
```

## Arguments

- mcs:

  fmcsR `mcs` result object.

## Value

List of atom-map data frames.
