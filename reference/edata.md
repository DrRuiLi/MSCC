# Edge attribute table for graph-like objects

Edge attribute table for graph-like objects

## Usage

``` r
edata(object)
```

## Arguments

- object:

  Graph-like object (for example an `igraph`).

## Value

Data frame of edge attributes.
